import { createPageMetadata } from "../../seoConfig";

export const metadata = createPageMetadata("/pages/OurSchoolPolicies");

export default function PoliciesLayout({ children }) {
  return children;
}
