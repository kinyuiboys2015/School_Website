export const metadata = {
  title: "Password Recovery",
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
      noarchive: true,
    },
  },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
