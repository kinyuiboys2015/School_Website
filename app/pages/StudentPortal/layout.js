import { createPageMetadata } from "../../seoConfig";

export const metadata = createPageMetadata("/pages/StudentPortal");

export default function StudentPortalLayout({ children }) {
  return children;
}
