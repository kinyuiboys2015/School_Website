import { createPageMetadata } from "../../seoConfig";

export const metadata = createPageMetadata("/pages/eventsandnews");

export default function EventsAndNewsLayout({ children }) {
  return children;
}
